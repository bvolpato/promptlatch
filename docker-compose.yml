services:
  promptlatch:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      PROMPTLATCH_TARGET_BASE_URL: https://api.openai.com/v1
      PROMPTLATCH_TARGET_API_KEY: ${OPENAI_API_KEY}
