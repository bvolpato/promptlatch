replicaCount: 1

image:
  repository: ghcr.io/bvolpato/promptlatch
  tag: "0.2.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

serverAuth:
  enabled: true
  apiKey: ""

migration:
  # Set only while upgrading a release installed from the old chart.
  preserveLegacyNames: false

# Existing Secret keys are loaded as environment variables. Include
# PROMPTLATCH_SERVER_API_KEY when serverAuth.enabled is true. The 0.2 chart also
# accepts the previous variable name during upgrades.
existingSecret: ""

env:
  PROMPTLATCH_HOST: 0.0.0.0
  PROMPTLATCH_PORT: "8000"
  PROMPTLATCH_TARGET_DEFAULT_BASE_URL: https://api.openai.com/v1

secretEnv: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 10001
  runAsGroup: 10001
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: ["ALL"]

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: "1"
    memory: 512Mi
